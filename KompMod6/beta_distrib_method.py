import random
import numpy as np

def first(u, v):
    """for u, v - integer
    """
    rand_arr = []
    arr = np.zeros_like(dtype=float)
    for i in range(u + v - 1):
        rand_arr.append(random.random())

    random_arr.sort()

    return 

def second(u, v):
    """for u, v - positive
    """